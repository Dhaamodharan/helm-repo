apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartbox.fullname" . }}-test-connection"
  labels:
    {{- include "smartbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
