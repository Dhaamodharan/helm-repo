apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendorbox.fullname" . }}
  labels:
    {{- include "vendorbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vendorbox.selectorLabels" . | nindent 4 }}
