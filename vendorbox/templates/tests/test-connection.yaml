apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vendorbox.fullname" . }}-test-connection"
  labels:
    {{- include "vendorbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vendorbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
