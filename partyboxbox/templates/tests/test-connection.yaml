apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "partyboxbox.fullname" . }}-test-connection"
  labels:
    {{- include "partyboxbox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "partyboxbox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
