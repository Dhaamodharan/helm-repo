apiVersion: v1
kind: Service
metadata:
  name: {{ include "partyboxbox.fullname" . }}
  labels:
    {{- include "partyboxbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "partyboxbox.selectorLabels" . | nindent 4 }}
